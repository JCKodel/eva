/// This enum is used to filter the to-dos in our ui
enum ListToDosFilter {
  /// Show all to-dos
  all,

  /// Show only to-dos that are completed
  completedOnly,

  /// Show only to-dos that are not completed
  uncompletedOnly,
}
