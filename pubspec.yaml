name: eva
description: Event Architecture - A multithread event driven clean architecture framework
version: 1.2.0
homepage: https://github.com/JCKodel/eva

environment:
  sdk: ">=2.19.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  kfx_dependency_injection: ^1.5.1+2
  meta: ^1.8.0
  rxdart: ^0.27.7

dev_dependencies:
  flutter_lints: ^2.0.0

flutter: null
